#![allow(unused)]
// Using a semicolon after mod front_of_house rather than using a block tells
// Rust to load the contents of the module from another file with
// the same name as the module.
pub fn add_to_waitlist() {}
